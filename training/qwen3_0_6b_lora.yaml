### LLaMA-Factory training placeholder; review and expand the dataset before running.
model_name_or_path: Qwen/Qwen3-0.6B
trust_remote_code: true
stage: sft
do_train: true
finetuning_type: lora
lora_target: all
dataset_dir: training
# 审核完成后可改为：robot_dialogue_seed,robot_dialogue_lora_approved
# 不要直接把 robot_dialogue_lora_candidates 当作训练集，候选样例必须先人工审核。
dataset: robot_dialogue_seed
template: qwen3_nothink
cutoff_len: 1024
max_samples: 10000
overwrite_cache: true
preprocessing_num_workers: 4
output_dir: outputs/qwen3-0.6b-robot-lora
logging_steps: 10
save_steps: 100
plot_loss: true
overwrite_output_dir: true
per_device_train_batch_size: 2
gradient_accumulation_steps: 8
learning_rate: 0.0001
num_train_epochs: 3.0
lr_scheduler_type: cosine
warmup_ratio: 0.1
bf16: true
ddp_timeout: 180000000
